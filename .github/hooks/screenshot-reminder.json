{
  "hooks": {
    "PostToolUse": [
      {
        "type": "command",
        "command": "changed=$(echo \"$COPILOT_TOOL_RESULT\" 2>/dev/null | grep -oE 'src/hash_turbo/gui/qml/[^ \"]+\\.qml' | head -1); if [ -n \"$changed\" ]; then echo '{\"systemMessage\": \"QML file changed — consider regenerating documentation screenshots with @screenshot-docs.\"}'; fi",
        "timeout": 5
      }
    ]
  }
}
